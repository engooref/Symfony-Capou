{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

	<div class="mb-3">
        <p class="account"><strong><u> Email</u></strong> : {{ app.user.username }}</p>
        </br>
        <p class="account"><strong><u> Mdp (crypted)</u></strong> : {{ app.user.password }}</p>
        </br>
        <p class="account"><strong><u> Role</u></strong> : {% for user in app %}
					<li>{{ user.roles }}</li>
                {% endfor %}</p>
    </div>
</div>
{% endblock %}
